# This `.xcode.env` file is versioned and sourced by Xcode script phases.
# Do NOT hardcode secrets or keys here. Use `.xcode.env.local` for local overrides.

# Automatically use the current Node.js binary in the environment
export NODE_BINARY=$(command -v node)

# [Optional] To support nvm usage (uncomment if using nvm + brew)
# . "$(brew --prefix nvm)/nvm.sh"
# nvm use node